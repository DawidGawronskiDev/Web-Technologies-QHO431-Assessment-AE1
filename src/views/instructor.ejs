<%- include("./partials/root-layout", { title: `UpSpark | ${instructor.name}`,
content: ` ${include('./partials/instructor-section')}
${include('./partials/breadcrumbs.ejs', { breadcrumbs: [ { name: "Home", href:
"/" }, { name: "Instructors", href: "/instructors" }, { name:
"${instructor.name}", href: null } ] })} ${ courses && courses.length > 0 ?
include('./partials/courses-section', { title: `Courses by ${instructor.name}`,
description: `Explore Our Available Courses created by ${instructor.name}.`,
layout: 'list-layout', courses }) : null } ${ events && events.length > 0 ?
include('./partials/events-section', { title: `Events by ${instructor.name}`,
description: `Explore upcoming and past events hosted by ${instructor.name}.`,
layout: 'list-layout', events }) : null } ` }) %>
